{
    "session-config": {
        "tokenKey": "secure_jwt_secret_for_testing_milestone2",
        "tokenTTL": 3000
    },
    "database-config": {
        "url": "mongodb://localhost:27017/rest-server"
    },
    "server-config": {
        "http": {
            "port": 80
        },
        "https": {
            "port": 443,
            "certPath": "./certificates/cert.pem",
            "keyPath": "./certificates/key.pem"
        }
    }
}
